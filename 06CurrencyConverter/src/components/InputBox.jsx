import React from 'react'

const InputBox = (
    label,
    amount,
    onAmountChange,
    onCurrencyChange,
    currencyOptions=[],
    selectedCurrency="usd",
    amountDisabled=false,
    currencyDesiabled=false,
    className="",

) => {
  return (
    <div>InputBox</div>
  )
}

export default InputBox;